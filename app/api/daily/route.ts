import {NextRequest,NextResponse} from "next/server";
import {appendSheetValues,clearAndWrite,ensureSheet,getSheetRanges} from "@/lib/google-sheets";

const SOURCE_ID="160_eV8tgT_eXH7dm8pHP8Ym2mHPyHhlFpKWf1bpxEP0";
const MASTER_ID="1v479QFSArfDb-vt_YRGcw0o4RhYxCzFlNOCH6VMvCSk";
const STORE="M238";
const SNAPSHOT="Dashboard Schedule Snapshot",TARGET_SNAPSHOT="Dashboard Daily Target Snapshot",SALES_SNAPSHOT="Dashboard Daily Sales Snapshot";
const SALES_HEADERS=["Date","Sales ID","Sales Name","Position","Share","Status","Target Amount","Target Accessories","Target VAS","Achievement Amount","Accessories","VAS","Qty","Invoices","Updated At"];
const n=(v:unknown)=>typeof v==="number"?v:Number(String(v??"").replace(/[^0-9.-]/g,""))||0;
const s=(v:unknown)=>String(v??"").trim(),up=(v:unknown)=>s(v).toUpperCase();
function iso(v:unknown){const x=s(v);if(/^\d{2}-\d{2}-\d{4}$/.test(x)){const[d,m,y]=x.split("-");return`${y}-${m}-${d}`}if(/^\d{4}-\d{2}-\d{2}/.test(x))return x.slice(0,10);if(typeof v==="number")return new Date(Date.UTC(1899,11,30)+v*86400000).toISOString().slice(0,10);return""}
function todayJakarta(){return new Intl.DateTimeFormat("sv-SE",{year:"numeric",month:"2-digit",day:"2-digit",timeZone:"Asia/Jakarta"}).format(new Date())}
function weekday(date:string){return new Intl.DateTimeFormat("id-ID",{weekday:"long",timeZone:"Asia/Jakarta"}).format(new Date(`${date}T00:00:00Z`)).toLowerCase()}
function unavailable(status:string){return /OFF|CUTI|TRAINING|MEETING|\bRO\b|SAKIT|SICK|LEAVE/i.test(status)}
function isSupervisor(position:string){return /SUPERVISOR|\bSPV\b/i.test(position)}
function isDigimapOnline(name:string){return /DIGIMAP\s*\.?\s*CO\s*\.?\s*ID/i.test(name)}
function shareValue(v:unknown){const x=n(v);return x>1?x/100:Math.max(0,x)}
function neutralLabel(status:string){const x=up(status);if(/OFF/.test(x))return"OFF";if(/CUTI|LEAVE/.test(x))return"Cuti";if(/TRAINING/.test(x))return"Training";if(/MEETING/.test(x))return"Meeting";if(/SAKIT|SICK/.test(x))return"Sakit";if(/\bRO\b/.test(x))return"RO";return s(status)||"IN"}

const SHIFT:Record<string,{start:number;end:number}>={D047:{start:9*60,end:17*60},D072:{start:11*60,end:19*60},D083:{start:12*60,end:20*60},D093:{start:13*60,end:21*60},D103:{start:14*60,end:22*60}};
function nowJakartaMinutes(){const parts=new Intl.DateTimeFormat("en-GB",{timeZone:"Asia/Jakarta",hour:"2-digit",minute:"2-digit",hour12:false}).formatToParts(new Date()),h=Number(parts.find(x=>x.type==="hour")?.value||0),m=Number(parts.find(x=>x.type==="minute")?.value||0);return h*60+m}
function liveMeta(date:string,status:string,amount:number,target:number){if(unavailable(status))return{liveStatus:neutralLabel(status),statusTone:"neutral",shiftStart:"",shiftEnd:"",shiftProgress:0,paceTarget:0};const code=up(status),shift=SHIFT[code];if(!shift)return{liveStatus:amount>=target&&target>0?"Achieve":amount>0?"Need Push":"Belum Produktif",statusTone:amount>=target&&target>0?"green":amount>0?"yellow":"neutral",shiftStart:"",shiftEnd:"",shiftProgress:0,paceTarget:0};const hh=(v:number)=>`${String(Math.floor(v/60)).padStart(2,"0")}.${String(v%60).padStart(2,"0")}`,today=todayJakarta();if(date!==today){const achieved=target>0&&amount>=target;return{liveStatus:achieved?"Achieve":amount>0?"Need Push":"Belum Produktif",statusTone:achieved?"green":amount>0?"yellow":"red",shiftStart:hh(shift.start),shiftEnd:hh(shift.end),shiftProgress:1,paceTarget:target}}const now=nowJakartaMinutes();if(now<shift.start)return{liveStatus:"Belum Masuk Shift",statusTone:"neutral",shiftStart:hh(shift.start),shiftEnd:hh(shift.end),shiftProgress:0,paceTarget:0};const elapsed=Math.min(Math.max(0,now-shift.start),shift.end-shift.start),progress=Math.min(1,elapsed/(shift.end-shift.start)),paceTarget=target*progress;if(target>0&&amount>=paceTarget)return{liveStatus:"Achieve",statusTone:"green",shiftStart:hh(shift.start),shiftEnd:hh(shift.end),shiftProgress:progress,paceTarget};if(amount>0)return{liveStatus:"Need Push",statusTone:"yellow",shiftStart:hh(shift.start),shiftEnd:hh(shift.end),shiftProgress:progress,paceTarget};if(elapsed<120)return{liveStatus:"Belum Produktif",statusTone:"neutral",shiftStart:hh(shift.start),shiftEnd:hh(shift.end),shiftProgress:progress,paceTarget};if(elapsed<=240)return{liveStatus:"Need Push",statusTone:"yellow",shiftStart:hh(shift.start),shiftEnd:hh(shift.end),shiftProgress:progress,paceTarget};return{liveStatus:"Belum Produktif",statusTone:"red",shiftStart:hh(shift.start),shiftEnd:hh(shift.end),shiftProgress:progress,paceTarget}}

type Row={date:string;id:string;name:string;invoice:string;article:string;description:string;type:string;qty:number;amount:number;category:string;brand:string;core:string;scheme:string;vendor:string};
function parse(r:unknown[]):Row{return{date:iso(r[0]),id:s(r[1]),name:s(r[2]),invoice:s(r[3]),article:s(r[4]),description:s(r[5]),type:s(r[6]),qty:n(r[7]),amount:n(r[8]),category:up(r[9]),brand:up(r[10]),core:up(r[11]),scheme:up(r[12]),vendor:up(r[13])}}
function valid(r:Row){return!!(r.id&&r.date)&&r.scheme!=="VOUCHER"&&!up(r.description).includes("VOUCHER")}
function kind(r:Row){const text=`${r.category} ${up(r.type)} ${up(r.description)}`;if(r.scheme==="VAS")return"vas";if(r.scheme==="ACCESSORIES")return"accessories";if(r.scheme==="DEVICES")return"device";if(/IPHONE|IPAD|MAC|APPLE WATCH|AIRPODS/.test(text))return"device";return"other"}
function product(r:Row){if(r.brand!=="APPLE")return"other";if(r.scheme==="ACCESSORIES")return r.category==="AIRPODS"?"airpods":"other";if(r.scheme!=="DEVICES")return"other";if(r.category==="IPHONE")return"iphone";if(r.category==="MAC")return"mac";if(r.category==="IPAD")return"ipad";if(r.category==="APPLE WATCH")return"watch";return"other"}
function vasType(r:Row){const t=`${r.article} ${r.brand} ${r.vendor} ${r.description}`.toUpperCase();if(t.includes("QOALA")||t.includes("KLA"))return"qoala";if(t.includes("TELKOMSEL")||t.includes("TSL"))return"telkomsel";if(t.includes("INDOSAT")||t.includes("IDT"))return"indosat";if(/(^|\s)XL(\s|$)|XXL/.test(t))return"xl";return""}

type StaffBase={id:string;name:string;position:string;share:number;status:string;configShare?:number};
type DailyTarget={amount:number;accessories:number;vas:number};
type DailyStaff=StaffBase&{amount:number;accessories:number;vas:number;qty:number;invoices:number;upt:number;atv:number;targets:DailyTarget;lob:{iphone:number;mac:number;ipad:number;watch:number;airpods:number};vasDetail:{qoala:{qty:number;value:number};telkomsel:{qty:number;value:number};xl:{qty:number;value:number};indosat:{qty:number;value:number}};liveStatus?:string;statusTone?:string;shiftStart?:string;shiftEnd?:string;shiftProgress?:number;paceTarget?:number};

async function ensureMigrated(tab:string,headers:string[],range:string,appendRange:string,email:string,key:string){await ensureSheet(MASTER_ID,tab,headers,email,key);const[master]=await getSheetRanges(MASTER_ID,[range],email,key);if(master.some(r=>r.some(v=>s(v))))return master;try{const[legacy]=await getSheetRanges(SOURCE_ID,[range],email,key),rows=legacy.filter(r=>r.some(v=>s(v)));if(rows.length)await appendSheetValues(MASTER_ID,appendRange,rows,email,key);return rows}catch{return master}}
function snapshotStaff(rows:unknown[][]):DailyStaff[]{return rows.map(r=>{const amount=n(r[9]),qty=n(r[12]),invoices=n(r[13]);return{id:s(r[1]),name:s(r[2]),position:s(r[3]),share:n(r[4]),status:up(r[5])||"IN",targets:{amount:n(r[6]),accessories:n(r[7]),vas:n(r[8])},amount,accessories:n(r[10]),vas:n(r[11]),qty,invoices,upt:invoices?qty/invoices:0,atv:invoices?amount/invoices:0,lob:{iphone:0,mac:0,ipad:0,watch:0,airpods:0},vasDetail:{qoala:{qty:0,value:0},telkomsel:{qty:0,value:0},xl:{qty:0,value:0},indosat:{qty:0,value:0}}}}).filter(r=>r.id&&!isSupervisor(r.position))}
async function readSalesSnapshot(date:string,email:string,key:string){const rows=await ensureMigrated(SALES_SNAPSHOT,SALES_HEADERS,`'${SALES_SNAPSHOT}'!A2:O5000`,`'${SALES_SNAPSHOT}'!A:O`,email,key);return{all:rows,dated:rows.filter(r=>iso(r[0])===date)}}
function snapshotValues(date:string,row:DailyStaff){return[date,row.id,row.name,row.position,row.share,row.status,row.targets.amount,row.targets.accessories,row.targets.vas,row.amount,row.accessories,row.vas,row.qty,row.invoices,new Date().toISOString()]}
function snapshotChanged(saved:unknown[],next:unknown[]){return next.slice(0,14).some((value,index)=>index===0||index===1||index===2||index===3||index===5?s(saved[index])!==s(value):n(saved[index])!==n(value))}
async function saveSalesSnapshot(date:string,staff:DailyStaff[],all:unknown[][],email:string,key:string){const indexed=new Map(all.map((row,index)=>[`${iso(row[0])}:${s(row[1])}`,{row,index:index+2}])),missing:unknown[][]=[];for(const person of staff){const values=snapshotValues(date,person),saved=indexed.get(`${date}:${person.id}`);if(!saved)missing.push(values);else if(snapshotChanged(saved.row,values))await clearAndWrite(MASTER_ID,null,`'${SALES_SNAPSHOT}'!A${saved.index}:O${saved.index}`,[values],email,key,"RAW")}if(missing.length)await appendSheetValues(MASTER_ID,`'${SALES_SNAPSHOT}'!A:O`,missing,email,key)}
async function targetForDate(date:string,configRows:unknown[][],email:string,key:string):Promise<{target:DailyTarget;locked:boolean}>{const existing=await ensureMigrated(TARGET_SNAPSHOT,["Date","Amount","Accessories","VAS","Created At"],`'${TARGET_SNAPSHOT}'!A2:E3000`,`'${TARGET_SNAPSHOT}'!A:E`,email,key);const dailyTargetRow=configRows.find(r=>s(r[22]).toLowerCase()===weekday(date));const configured={amount:n(dailyTargetRow?.[23]),accessories:n(dailyTargetRow?.[24]),vas:n(dailyTargetRow?.[25])};const saved=existing.find(r=>iso(r[0])===date);if(saved){const savedTarget={amount:n(saved[1]),accessories:n(saved[2]),vas:n(saved[3])};const target=(configured.amount||configured.accessories||configured.vas)?configured:savedTarget;return{target,locked:true}}const shouldLock=date<=todayJakarta()&&(configured.amount>0||configured.accessories>0||configured.vas>0);if(shouldLock)await appendSheetValues(MASTER_ID,`'${TARGET_SNAPSHOT}'!A:E`,[[date,configured.amount,configured.accessories,configured.vas,new Date().toISOString()]],email,key);return{target:configured,locked:shouldLock}}

const MONTHS:Record<string,number>={JANUARI:1,FEBRUARI:2,MARET:3,APRIL:4,MEI:5,JUNI:6,JULI:7,AGUSTUS:8,SEPTEMBER:9,OKTOBER:10,NOVEMBER:11,DESEMBER:12};
function monthFromRow(row:unknown[]){for(const cell of row.slice(0,3)){const m=MONTHS[up(cell)];if(m)return m}return 0}
function pad2(v:number){return String(v).padStart(2,"0")}
function scheduleBlockMatches(date:string,month:number,days:number[]){const[y,m,d]=date.split("-").map(Number);if(!month||!days.includes(d))return-1;const wrap=days.findIndex((v,i)=>i<days.length-1&&v>days[i+1]);for(let i=0;i<days.length;i++){if(days[i]!==d)continue;let cellMonth=month;if(wrap>=0&&i<=wrap)cellMonth=month===1?12:month-1;let cellYear=y;if(month===1&&cellMonth===12)cellYear=y-1;if(cellMonth===m&&`${cellYear}-${pad2(cellMonth)}-${pad2(d)}`===date)return i}return-1}
function carryStatus(row:unknown[],dayIndex:number){let value=up(row[3+dayIndex]);if(value)return value;for(let i=dayIndex-1;i>=0;i--){const prev=up(row[3+i]);if(!prev)continue;if(/TRAINING|MEETING|CUTI|SAKIT|SICK|LEAVE|\bRO\b/.test(prev))return prev;break}return"IN"}
async function scheduleFor(date:string,email:string,key:string):Promise<{staff:StaffBase[];source:string;remark:string}>{await ensureSheet(MASTER_ID,SNAPSHOT,["Date","Sales ID","Sales Name","Position","Share","Status","Created At"],email,key);await ensureSheet(MASTER_ID,SALES_SNAPSHOT,SALES_HEADERS,email,key);const[weekly,snapshots,salesRows]=await getSheetRanges(MASTER_ID,["'Schedule weekly'!A1:Q600",`'${SNAPSHOT}'!A2:G5000`,`'${SALES_SNAPSHOT}'!A2:O5000`],email,key);const nameToId=new Map<string,string>();for(const r of weekly){const id=s(r[0]),name=s(r[1]);if(/^\d{6,10}$/.test(id)&&name)nameToId.set(up(name),id)}const salesNameToId=new Map<string,string>();for(const r of salesRows){const id=s(r[1]),name=s(r[2]);if(/^\d{6,10}$/.test(id)&&name)salesNameToId.set(up(name),id)}for(let r=0;r<weekly.length-2;r++){const month=monthFromRow(weekly[r]);if(!month)continue;const days=weekly[r].slice(3,10).map(v=>n(v)).filter(v=>v>0&&v<=31);if(days.length!==7)continue;const dayIndex=scheduleBlockMatches(date,month,days);if(dayIndex<0)continue;let header=r+1;if(up(weekly[header]?.[1])!=="NAME"&&up(weekly[header]?.[0])!=="NIK"){header=r+2;if(up(weekly[header]?.[1])!=="NAME"&&up(weekly[header]?.[0])!=="NIK")continue}const staff:StaffBase[]=[];for(let i=header+1;i<Math.min(weekly.length,header+24);i++){const row=weekly[i],first=up(row?.[0]),name=s(row?.[1]);if(!name)continue;if(/^D0\d{2}|FULL|OFF|CUTI/.test(first)||/^D0\d{2}|FULL|OFF|CUTI/.test(up(name)))break;const position=s(row?.[2])||"Sales Assistant";let id=s(row?.[0]);if(!/^\d{6,10}$/.test(id))id=nameToId.get(up(name))||salesNameToId.get(up(name))||"";if(!id)continue;staff.push({id,name,position,share:0,status:carryStatus(row,dayIndex)})}if(staff.length)return{staff,source:"master-schedule-weekly",remark:`Schedule ${date} dibaca langsung dari MASTER DATA M238 → Schedule weekly.`}}const locked=snapshots.filter(r=>iso(r[0])===date).map(r=>({id:s(r[1]),name:s(r[2]),position:s(r[3])||"Sales Assistant",share:0,status:up(r[5])||"IN"})).filter(r=>r.id);if(locked.length)return{staff:locked,source:"master-snapshot",remark:"Schedule weekly tanggal ini belum ditemukan; memakai snapshot MASTER DATA M238."};const roster=new Map<string,StaffBase>();for(const r of salesRows){const id=s(r[1]),name=s(r[2]);if(!/^\d{6,10}$/.test(id)||!name)continue;const prev=roster.get(id);roster.set(id,{id,name,position:s(r[3])||prev?.position||"Sales Assistant",share:0,status:"IN"})}return{staff:[...roster.values()],source:"master-fallback",remark:"Schedule weekly tanggal ini belum tersedia; roster tetap berasal dari MASTER DATA M238."}}
function aggregatePerson(base:StaffBase,mine:Row[],targets:DailyTarget):DailyStaff{const invoices=new Set(mine.map(r=>r.invoice).filter(Boolean)),sum=(k:string)=>mine.filter(r=>kind(r)===k).reduce((a,r)=>a+r.amount,0),qty=mine.reduce((a,r)=>a+r.qty,0),amount=mine.reduce((a,r)=>a+r.amount,0),lob={iphone:0,mac:0,ipad:0,watch:0,airpods:0},vasDetail={qoala:{qty:0,value:0},telkomsel:{qty:0,value:0},xl:{qty:0,value:0},indosat:{qty:0,value:0}};for(const r of mine){const pr=product(r);if(pr in lob)lob[pr as keyof typeof lob]+=r.qty;const vt=vasType(r);if(vt){vasDetail[vt as keyof typeof vasDetail].qty+=r.qty;vasDetail[vt as keyof typeof vasDetail].value+=r.amount}}return{...base,amount,accessories:sum("accessories"),vas:sum("vas"),qty,invoices:invoices.size,upt:invoices.size?qty/invoices.size:0,atv:invoices.size?amount/invoices.size:0,targets,lob,vasDetail}}
function targetsByShare(target:DailyTarget,share:number):DailyTarget{return{amount:target.amount*share,accessories:target.accessories*share,vas:target.vas*share}}

export async function GET(req:NextRequest){const email=process.env.GOOGLE_SERVICE_ACCOUNT_EMAIL,key=process.env.GOOGLE_PRIVATE_KEY;if(!email||!key)return NextResponse.json({error:"Koneksi Google Sheets belum tersedia"},{status:500});const date=req.nextUrl.searchParams.get("date")||todayJakarta();try{const[configRows,rawDates,compiledDates]=await getSheetRanges(SOURCE_ID,["Config!A1:AZ120","'RAW SalesPerson'!AB2:AB65536","'Data Copas'!A2:A50000"],email,key);const shareById=new Map<string,number>(),shareByName=new Map<string,number>();for(const r of configRows.slice(27,55)){if(up(r[7])!==STORE||!s(r[8])||!s(r[9])||isSupervisor(s(r[10])))continue;const sh=shareValue(r[11]);shareById.set(s(r[8]),sh);shareByName.set(up(r[9]),sh)}const[scheduleResult,targetResult,salesSnapshot]=await Promise.all([scheduleFor(date,email,key),targetForDate(date,configRows,email,key),readSalesSnapshot(date,email,key)]);const scheduled=scheduleResult.staff.filter(p=>!isSupervisor(p.position)).map(p=>({...p,configShare:shareById.get(p.id)??shareByName.get(up(p.name))??0})),eligibleScheduled=scheduled.filter(p=>!unavailable(p.status)&&p.configShare!>0),shareTotal=eligibleScheduled.reduce((a,p)=>a+(p.configShare||0),0),withShares=scheduled.map(p=>({...p,share:!unavailable(p.status)&&shareTotal>0?(p.configShare||0)/shareTotal:0}));const active=withShares.filter(p=>!unavailable(p.status));const matches:number[]=[],compiledMatches:number[]=[];rawDates.forEach((r,i)=>{if(iso(r[0])===date)matches.push(i+2)});compiledDates.forEach((r,i)=>{if(iso(r[0])===date)compiledMatches.push(i+2)});const sourceRange=matches.length?`'RAW SalesPerson'!AB${matches[0]}:AR${matches.at(-1)}`:compiledMatches.length?`'Data Copas'!A${compiledMatches[0]}:S${compiledMatches.at(-1)}`:"",raw=sourceRange?(await getSheetRanges(SOURCE_ID,[sourceRange],email,key))[0]??[]:[],rows=raw.map(parse).filter(r=>r.date===date&&valid(r));const target=targetResult.target,liveStaff:DailyStaff[]=active.map(p=>aggregatePerson(p,rows.filter(r=>r.id===p.id),targetsByShare(target,p.share)));const activeIds=new Set(active.map(p=>p.id)),scheduledById=new Map(withShares.map(p=>[p.id,p])),extraGroups=new Map<string,Row[]>();for(const r of rows){if(activeIds.has(r.id))continue;const group=extraGroups.get(r.id)||[];group.push(r);extraGroups.set(r.id,group)}const extraStaff:DailyStaff[]=[...extraGroups.entries()].map(([id,mine])=>{const p=scheduledById.get(id),online=isDigimapOnline(mine[0]?.name||"");return aggregatePerson({id,name:mine[0]?.name||p?.name||id,position:online?"Online Sales":p?.position||"Sales (non-schedule)",share:0,status:online?"ONLINE":p?.status||"EXTRA",configShare:p?.configShare||0},mine,{amount:0,accessories:0,vas:0})}).filter(p=>!isSupervisor(p.position));const currentStaff=[...liveStaff,...extraStaff],isPast=date<todayJakarta(),sourceAvailable=matches.length>0||compiledMatches.length>0,useSaved=isPast&&!sourceAvailable&&salesSnapshot.dated.length>0;let baseStaff=(useSaved?snapshotStaff(salesSnapshot.dated):currentStaff).filter(p=>!isSupervisor(p.position));if(useSaved){const eligible=baseStaff.filter(p=>!unavailable(p.status)&&p.status!=="EXTRA"&&p.status!=="ONLINE"),sumShare=eligible.reduce((a,p)=>a+(shareById.get(p.id)??shareByName.get(up(p.name))??0),0);baseStaff=baseStaff.map(p=>{const rawShare=shareById.get(p.id)??shareByName.get(up(p.name))??0,effective=!unavailable(p.status)&&sumShare>0?rawShare/sumShare:0;return effective>0?{...p,configShare:rawShare,share:effective,targets:targetsByShare(target,effective)}:{...p,configShare:rawShare}})}const staff=baseStaff.map(p=>({...p,...liveMeta(date,p.status,p.amount,p.targets.amount)}));if(!useSaved&&date<=todayJakarta())await saveSalesSnapshot(date,staff,salesSnapshot.all,email,key);const achv=staff.reduce((a,r)=>({amount:a.amount+r.amount,accessories:a.accessories+r.accessories,vas:a.vas+r.vas,qty:a.qty+r.qty,invoices:a.invoices+r.invoices}),{amount:0,accessories:0,vas:0,qty:0,invoices:0}),allocated=staff.filter(p=>p.share>0).reduce((a,p)=>a+p.targets.amount,0);return NextResponse.json({date,staff,total:{...achv,target:target.amount,accTarget:target.accessories,vasTarget:target.vas,upt:achv.invoices?achv.qty/achv.invoices:0},scheduleSource:scheduleResult.source,scheduleRemark:scheduleResult.remark,targetSource:"Config W:Z daily target + Config L28:L staff share",targetAllocation:{allocated,balance:target.amount-allocated,shareTotal},snapshotLocked:date<=todayJakarta(),targetSnapshotLocked:targetResult.locked,salesSnapshotLocked:date<=todayJakarta()&&(useSaved||staff.length>0),storage:"MASTER DATA M238",scheduleStorage:"MASTER DATA M238 → Schedule weekly"},{headers:{"cache-control":"no-store"}})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Gagal membaca daily sales"},{status:500})}}
