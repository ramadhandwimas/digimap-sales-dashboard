"use client";
import {useCallback,useEffect,useMemo,useRef,useState} from "react";
import {Bot,ChevronRight,Cloud,FileSpreadsheet,Gauge,LayoutDashboard,MessageSquare,Moon,RefreshCw,Settings,Sun,TrendingDown,TrendingUp,Upload,Users,WalletCards,WandSparkles} from "lucide-react";
import {Table,TableBody,TableCell,TableHead,TableHeader,TableRow} from "@/components/ui/table";
import type {CxMemberRecord,FeedbackRecord,M238Payload,StaffMetric} from "@/lib/m238-types";

const money=new Intl.NumberFormat("id-ID",{style:"currency",currency:"IDR",maximumFractionDigits:0}),num=new Intl.NumberFormat("id-ID"),pct=(v:number)=>`${new Intl.NumberFormat("id-ID",{maximumFractionDigits:1}).format(v)}%`,ach=(v:number,t:number)=>t?v/t*100:0;
const today=()=>new Intl.DateTimeFormat("sv-SE",{year:"numeric",month:"2-digit",day:"2-digit",timeZone:"Asia/Jakarta"}).format(new Date());
const currentPeriod=()=>today().slice(0,7),months=Array.from({length:12},(_,i)=>`2026-${String(i+1).padStart(2,"0")}`),monthName=(v:string)=>new Intl.DateTimeFormat("id-ID",{month:"long",year:"numeric"}).format(new Date(`${v}-01T00:00:00`));
type RefreshSetting="auto"|"60000"|"300000"|"600000";type ThemeStyle="classic"|"natural"|"worklife"|"happiness";type FontSize="small"|"normal"|"large";
type DailyStaff={id:string;name:string;position:string;share:number;status:string;amount:number;accessories:number;vas:number;qty:number;invoices:number;upt:number;atv:number;targets:{amount:number;accessories:number;vas:number};lob:{iphone:number;mac:number;ipad:number;watch:number;airpods:number};vasDetail:{qoala:{qty:number;value:number};telkomsel:{qty:number;value:number};xl:{qty:number;value:number};indosat:{qty:number;value:number}}};
type DailyPayload={date:string;staff:DailyStaff[];total:{amount:number;target:number;accessories:number;accTarget:number;vas:number;vasTarget:number;qty:number;invoices:number;upt:number};snapshotLocked:boolean;error?:string};

function Filter({label,value,onChange,children}:{label:string;value:string;onChange:(v:string)=>void;children:React.ReactNode}){return <label className="block"><span className="mb-1.5 block text-xs font-bold uppercase tracking-[.12em] text-slate-400">{label}</span><select value={value} onChange={e=>onChange(e.target.value)} className="h-11 w-full rounded-xl border bg-white px-3 text-sm font-semibold text-slate-700 outline-none focus:ring-2 focus:ring-blue-200 dark:bg-slate-900 dark:text-slate-100">{children}</select></label>}
function Card({label,value,sub,tone="blue",icon:Icon=TrendingUp}:{label:string;value:string;sub:string;tone?:"blue"|"green"|"amber"|"violet"|"rose";icon?:typeof TrendingUp}){const c={blue:"bg-blue-50 text-blue-600",green:"bg-emerald-50 text-emerald-600",amber:"bg-amber-50 text-amber-600",violet:"bg-violet-50 text-violet-600",rose:"bg-rose-50 text-rose-600"}[tone];return <article className="min-w-0 rounded-2xl border bg-white p-5 shadow-sm dark:bg-slate-950"><div className="flex items-start justify-between gap-2"><p className="text-sm font-semibold text-slate-500">{label}</p><span className={`shrink-0 rounded-xl p-2.5 ${c}`}><Icon className="size-5"/></span></div><p className="mt-4 break-words text-xl font-black tracking-[-.035em] sm:text-2xl">{value}</p><p className="mt-3 border-t border-dashed pt-3 text-xs text-slate-500">{sub}</p></article>}
function SectionTitle({title,sub,right}:{title:string;sub:string;right?:React.ReactNode}){return <div className="flex items-center justify-between gap-4 border-b px-5 py-4"><div><h2 className="font-extrabold">{title}</h2><p className="mt-1 text-sm text-slate-500">{sub}</p></div>{right}</div>}
function Empty({text="Belum ada data pada filter ini."}:{text?:string}){return <TableRow><TableCell colSpan={20} className="h-28 text-center text-slate-400">{text}</TableCell></TableRow>}
function PeriodFilter({period,setPeriod}:{period:string;setPeriod:(v:string)=>void}){return <section className="mb-5 rounded-2xl border bg-white p-4 shadow-sm dark:bg-slate-950"><div className="max-w-xs"><Filter label="Filter Bulan" value={period} onChange={setPeriod}>{months.map(x=><option key={x} value={x}>{monthName(x)}</option>)}</Filter></div></section>}
function Bar({label,value,max}:{label:string;value:number;max:number}){const w=max?Math.max(2,value/max*100):0;return <div><div className="mb-2 flex justify-between gap-3 text-sm"><span className="font-semibold">{label}</span><span className="font-bold">{money.format(value)}</span></div><div className="h-3 overflow-hidden rounded-full bg-slate-100 dark:bg-slate-800"><div className="h-full rounded-full bg-blue-600" style={{width:`${Math.min(100,w)}%`}}/></div></div>}

export default function DashboardV3(){
 const[tab,setTab]=useState("dashboard"),[period,setPeriod]=useState(months.includes(currentPeriod())?currentPeriod():"2026-09"),[staff,setStaff]=useState("ALL"),[data,setData]=useState<M238Payload|null>(null),[loading,setLoading]=useState(true),[dark,setDark]=useState(false),[refreshSetting,setRefreshSetting]=useState<RefreshSetting>("auto"),[themeStyle,setThemeStyle]=useState<ThemeStyle>("worklife"),[fontSize,setFontSize]=useState<FontSize>("normal"),[message,setMessage]=useState(""),[invoice,setInvoice]=useState(""),[saving,setSaving]=useState(false),[uploadStatus,setUploadStatus]=useState(""),fileRef=useRef<HTMLInputElement>(null),uploadingRef=useRef(false);
 useEffect(()=>{const d=localStorage.getItem("m238-theme")==="dark";setDark(d);document.documentElement.classList.toggle("dark",d);const r=(localStorage.getItem("m238-refresh")||"auto") as RefreshSetting;if(["auto","60000","300000","600000"].includes(r))setRefreshSetting(r);const t=(localStorage.getItem("m238-style")||"worklife") as ThemeStyle;if(["classic","natural","worklife","happiness"].includes(t))setThemeStyle(t);const f=(localStorage.getItem("m238-font")||"normal") as FontSize;if(["small","normal","large"].includes(f))setFontSize(f)},[]);
 const load=useCallback(async(force=false)=>{setLoading(true);try{const r=await fetch(`/api/data?period=${period}${force?`&refresh=1&t=${Date.now()}`:""}`,force?{cache:"no-store"}:undefined);setData(await r.json())}finally{setLoading(false)}},[period]);
 useEffect(()=>{void load();const delay=refreshSetting==="auto"?60000:Number(refreshSetting),timer=setInterval(()=>{if(!uploadingRef.current&&(refreshSetting!=="auto"||document.visibilityState==="visible"))void load()},delay);return()=>clearInterval(timer)},[load,refreshSetting]);
 const toggleTheme=()=>{const v=!dark;setDark(v);document.documentElement.classList.toggle("dark",v);localStorage.setItem("m238-theme",v?"dark":"light")},changeRefresh=(v:RefreshSetting)=>{setRefreshSetting(v);localStorage.setItem("m238-refresh",v)},changeTheme=(v:ThemeStyle)=>{setThemeStyle(v);localStorage.setItem("m238-style",v)},changeFont=(v:FontSize)=>{setFontSize(v);localStorage.setItem("m238-font",v)};
 const upload=async()=>{const file=fileRef.current?.files?.[0];if(!file)return setMessage("Pilih file SPW terlebih dahulu.");setSaving(true);uploadingRef.current=true;setUploadStatus("Mengunggah…");try{const body=new FormData();body.append("file",file);const r=await fetch("/api/upload-spw",{method:"POST",body}),j=await r.json();if(!r.ok)throw new Error(j.error);setMessage(`Upload ${num.format(j.rows)} baris berhasil.`);await load(true)}catch(e){setMessage(e instanceof Error?e.message:"Upload gagal")}finally{setSaving(false);uploadingRef.current=false;setUploadStatus("")}};
 const saveInvoice=async()=>{if(!invoice.trim())return;setSaving(true);try{const r=await fetch("/api/exchange",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({invoice})}),j=await r.json();if(!r.ok)throw new Error(j.error);setMessage(`Invoice ${j.invoice} berhasil disimpan.`);setInvoice("")}catch(e){setMessage(e instanceof Error?e.message:"Gagal menyimpan invoice")}finally{setSaving(false)}};
 const filtered=useMemo(()=>data?.monthlyStaff.filter(x=>staff==="ALL"||x.id===staff)??[],[data,staff]),nav=[['dashboard','Dashboard',LayoutDashboard],['daily','Daily Sales',TrendingUp],['staff','Staff Performance',Users],['incentive','Est. Incentive',WalletCards],['feedback','Feedback',MessageSquare],['cx','NPS/CX & Member',Cloud],['upload','Data Upload',Upload],['settings','Settings',Settings]] as const;
 const theme={classic:{shell:"bg-slate-50",side:"bg-[#086aad]"},natural:{shell:"bg-emerald-50/50",side:"bg-gradient-to-b from-emerald-600 to-teal-700"},worklife:{shell:"bg-slate-50",side:"bg-gradient-to-b from-slate-800 to-blue-900"},happiness:{shell:"bg-amber-50/60",side:"bg-gradient-to-b from-orange-500 via-rose-500 to-violet-600"}}[themeStyle],font={small:"text-[14px]",normal:"text-[16px]",large:"text-[18px]"}[fontSize];
 return <div className={`min-h-screen ${theme.shell} ${font} text-slate-950 dark:bg-slate-900 dark:text-slate-100 lg:grid lg:grid-cols-[236px_minmax(0,1fr)]`}><aside className={`${theme.side} border-b text-white lg:sticky lg:top-0 lg:h-screen lg:border-b-0`}><div className="flex h-full flex-col"><div className="p-5"><div className="flex items-center gap-3 border-b border-white/15 pb-5"><div className="grid size-11 place-items-center rounded-xl bg-white/15 font-black">M238</div><div><p className="font-extrabold">Digimap PIM 2</p><p className="text-[10px] font-bold uppercase tracking-[.18em] text-white/70">Performance Hub</p></div></div></div><nav className="grid grid-cols-4 gap-1 px-3 pb-3 lg:block lg:space-y-1">{nav.map(([k,l,I])=><button key={k} onClick={()=>setTab(k)} className={`flex items-center justify-center gap-2 rounded-xl px-3 py-3 text-xs font-bold lg:w-full lg:justify-start lg:text-sm ${tab===k?"bg-white/20 shadow-sm":"text-white/85 hover:bg-white/10"}`}><I className="size-4"/><span className="hidden sm:inline">{l}</span></button>)}</nav></div></aside><main className="min-w-0 pb-10"><header className="border-b bg-white/90 backdrop-blur-xl dark:bg-slate-950/90"><div className="flex items-center justify-between gap-4 px-4 py-3 sm:px-7"><div className="flex items-center gap-2 text-sm"><span className="text-slate-400">M238</span><ChevronRight className="size-3.5"/><b>{nav.find(x=>x[0]===tab)?.[1]}</b></div><div className="flex gap-2"><button onClick={toggleTheme} className="rounded-xl border bg-white p-2.5 dark:bg-slate-900">{dark?<Sun className="size-4"/>:<Moon className="size-4"/>}</button><button onClick={()=>void load(true)} className="flex items-center gap-2 rounded-xl border bg-white px-3 py-2 text-sm font-semibold dark:bg-slate-900"><RefreshCw className={`size-4 ${loading?"animate-spin":""}`}/>Perbarui</button></div></div></header><div className="px-4 pt-6 sm:px-7">{tab==="dashboard"&&<DashboardHome data={data} period={period} setPeriod={setPeriod}/>} {tab==="daily"&&<DailySales/>} {tab==="staff"&&<StaffPerformance data={data} period={period} setPeriod={setPeriod} staff={staff} setStaff={setStaff} rows={filtered}/>} {tab==="incentive"&&<Incentive data={data} period={period} setPeriod={setPeriod}/>} {tab==="feedback"&&<Feedback period={period}/>} {tab==="cx"&&<CxMember data={data} period={period}/>} {tab==="upload"&&<DataUpload upload={upload} saveInvoice={saveInvoice} fileRef={fileRef} invoice={invoice} setInvoice={setInvoice} saving={saving} uploadStatus={uploadStatus} message={message}/>} {tab==="settings"&&<SettingsPage data={data} dark={dark} toggleTheme={toggleTheme} refresh={refreshSetting} setRefresh={changeRefresh} theme={themeStyle} setTheme={changeTheme} font={fontSize} setFont={changeFont}/>}</div></main></div>}

function DashboardHome({data,period,setPeriod}:{data:M238Payload|null;period:string;setPeriod:(v:string)=>void}){const rows=data?.monthlyStaff??[],top=[...rows].sort((a,b)=>b.amount-a.amount).slice(0,3),bottom=[...rows].sort((a,b)=>a.amount-b.amount).slice(0,3),a=data?.annual,max=Math.max(...(a?.thisYear.map(x=>x.amount)??[0]),...(a?.lastYear.map(x=>x.amount)??[0]),1),s=data?.summary;return <div className="space-y-5"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-blue-600">M238 Performance Hub</p><h1 className="mt-2 text-3xl font-black">Dashboard</h1><p className="mt-1 text-sm text-slate-500">Ringkasan performa penjualan dan tren tahunan.</p></div><PeriodFilter period={period} setPeriod={setPeriod}/><section className="overflow-hidden rounded-3xl bg-gradient-to-br from-[#0872b9] to-[#075b97] p-5 text-white shadow-lg"><div className="grid gap-5 lg:grid-cols-[1fr_1.7fr] lg:items-center"><div><p className="text-sm text-blue-100">Welcome back, Dwimas</p><h2 className="mt-2 text-2xl font-black sm:text-3xl">M238 Sales Overview</h2></div><div className="grid grid-cols-2 gap-2.5">{[["MTD Sales",money.format(s?.amount??0)],["Estimate",money.format(s?.estimate??0)],["Achievement",pct(ach(s?.amount??0,data?.target.amount??0))],["Store Point",pct(s?.point??0)]].map(([l,v])=><div key={l} className="min-w-0 rounded-2xl border border-white/20 bg-white/10 p-3"><p className="text-[11px] text-blue-100">{l}</p><p className="mt-1 break-words text-base font-black leading-tight sm:text-lg">{v}</p></div>)}</div></div></section><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Card label="Device" value={money.format(s?.device??0)} sub="Revenue device MTD"/><Card label="Accessories" value={money.format(s?.accessories??0)} sub="Revenue accessories MTD" tone="violet"/><Card label="VAS" value={money.format(s?.vas??0)} sub="Revenue VAS MTD" tone="green"/><Card label="UPT / ATV" value={`${(s?.upt??0).toFixed(1)} / ${money.format(s?.atv??0)}`} sub="Produktivitas transaksi" tone="amber" icon={Gauge}/></section><section className="grid gap-5 xl:grid-cols-[1.5fr_1fr]"><div className="rounded-2xl border bg-white shadow-sm dark:bg-slate-950"><SectionTitle title="Revenue Analytics" sub="MTM, Last Year 2025, dan This Year 2026 secara value"/><div className="space-y-6 p-5"><div className="grid gap-3 sm:grid-cols-3"><Card label="This Month" value={money.format(a?.mtm.current??0)} sub="Sampai hari berjalan"/><Card label="Previous Month" value={money.format(a?.mtm.previous??0)} sub="Tanggal pembanding sama" tone="violet"/><Card label="MTM Growth" value={pct(a?.mtm.growth??0)} sub="Month to month" tone={(a?.mtm.growth??0)>=0?"green":"rose"} icon={(a?.mtm.growth??0)>=0?TrendingUp:TrendingDown}/></div><div><h3 className="mb-3 font-extrabold">Last Year 2025</h3><div className="space-y-3">{a?.lastYear.map(x=><Bar key={x.period} label={x.label} value={x.amount} max={max}/>)}</div><div className="mt-4 flex justify-between border-t pt-4 font-black"><span>Total 2025</span><span>{money.format(a?.lastYearTotal??0)}</span></div></div><div><h3 className="mb-3 font-extrabold">This Year 2026</h3><div className="space-y-3">{a?.thisYear.map(x=><Bar key={x.period} label={x.label} value={x.amount} max={max}/>)}</div><div className="mt-4 flex justify-between border-t pt-4 font-black"><span>Total 2026</span><span>{money.format(a?.thisYearTotal??0)}</span></div></div></div></div><div className="space-y-5"><Ranking title="Top Sales 3" rows={top}/><Ranking title="Bottom Sales 3" rows={bottom} bottom/></div></section></div>}
function Ranking({title,rows,bottom=false}:{title:string;rows:StaffMetric[];bottom?:boolean}){return <section className="rounded-2xl border bg-white shadow-sm dark:bg-slate-950"><SectionTitle title={title} sub={bottom?"3 achievement amount terendah":"3 achievement amount tertinggi"}/><div className="divide-y">{rows.map((r,i)=><div key={r.id} className="flex items-center gap-3 p-4"><div className={`grid size-9 place-items-center rounded-xl font-black ${bottom?"bg-rose-50 text-rose-600":"bg-blue-50 text-blue-600"}`}>{i+1}</div><div className="min-w-0 flex-1"><b className="block truncate">{r.name}</b><span className="text-xs text-slate-400">Achievement Amount</span></div><b className="text-sm">{money.format(r.amount)}</b></div>)}</div></section>}

function useDaily(date:string){const[daily,setDaily]=useState<DailyPayload|null>(null),[loading,setLoading]=useState(false);const load=useCallback(async()=>{setLoading(true);try{const r=await fetch(`/api/daily?date=${date}&t=${Date.now()}`,{cache:"no-store"});setDaily(await r.json())}finally{setLoading(false)}},[date]);useEffect(()=>{void load();const t=setInterval(()=>void load(),60000);return()=>clearInterval(t)},[load]);return{daily,loading,load}}
function DailySales(){const date=today(),{daily,loading,load}=useDaily(date),rows=daily?.staff??[],t=daily?.total;return <div className="space-y-5"><div className="flex items-start justify-between gap-4"><div><h1 className="text-3xl font-black">Daily Sales</h1><p className="mt-1 text-sm text-slate-500">Seluruh data mengikuti hari berjalan: {date}.</p></div><button onClick={()=>void load()} className="rounded-xl border bg-white p-2.5"><RefreshCw className={`size-4 ${loading?"animate-spin":""}`}/></button></div><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Card label="Target vs Achievement" value={`${money.format(t?.amount??0)} / ${money.format(t?.target??0)}`} sub={pct(ach(t?.amount??0,t?.target??0))}/><Card label="VAS vs Achievement" value={`${money.format(t?.vas??0)} / ${money.format(t?.vasTarget??0)}`} sub={pct(ach(t?.vas??0,t?.vasTarget??0))} tone="green"/><Card label="ACC vs Achievement" value={`${money.format(t?.accessories??0)} / ${money.format(t?.accTarget??0)}`} sub={pct(ach(t?.accessories??0,t?.accTarget??0))} tone="violet"/><Card label="UPT" value={(t?.upt??0).toFixed(1)} sub={`${num.format(t?.qty??0)} unit / ${num.format(t?.invoices??0)} transaksi`} tone="amber" icon={Gauge}/></section><section className="rounded-2xl border bg-white shadow-sm dark:bg-slate-950"><SectionTitle title="Daily Sales Staff" sub="Staff in-charge sesuai schedule hari berjalan"/><Table><TableHeader><TableRow><TableHead>Nama</TableHead><TableHead>Status</TableHead><TableHead className="text-right">Target</TableHead><TableHead className="text-right">Achievement</TableHead><TableHead className="text-right">%</TableHead><TableHead className="text-right">UPT</TableHead><TableHead className="text-right">ATV</TableHead></TableRow></TableHeader><TableBody>{rows.length?rows.map(r=><TableRow key={r.id}><TableCell><b>{r.name}</b><div className="text-xs text-slate-400">{r.position}</div></TableCell><TableCell>{r.status}</TableCell><TableCell className="text-right">{money.format(r.targets.amount)}</TableCell><TableCell className="text-right font-bold">{money.format(r.amount)}</TableCell><TableCell className="text-right">{pct(ach(r.amount,r.targets.amount))}</TableCell><TableCell className="text-right">{r.upt.toFixed(1)}</TableCell><TableCell className="text-right">{money.format(r.atv)}</TableCell></TableRow>):<Empty/>}{rows.length>0&&<TableRow className="bg-slate-50 font-black dark:bg-slate-900"><TableCell>TOTAL</TableCell><TableCell>—</TableCell><TableCell className="text-right">{money.format(t?.target??0)}</TableCell><TableCell className="text-right">{money.format(t?.amount??0)}</TableCell><TableCell className="text-right">{pct(ach(t?.amount??0,t?.target??0))}</TableCell><TableCell className="text-right">{(t?.upt??0).toFixed(1)}</TableCell><TableCell className="text-right">{money.format((t?.invoices??0)?(t?.amount??0)/(t?.invoices??1):0)}</TableCell></TableRow>}</TableBody></Table></section><DailyLob rows={rows}/><DailyVas rows={rows}/></div>}
function DailyLob({rows}:{rows:DailyStaff[]}){const t=rows.reduce((a,r)=>({iphone:a.iphone+r.lob.iphone,mac:a.mac+r.lob.mac,ipad:a.ipad+r.lob.ipad,watch:a.watch+r.lob.watch,airpods:a.airpods+r.lob.airpods}),{iphone:0,mac:0,ipad:0,watch:0,airpods:0});return <section className="rounded-2xl border bg-white shadow-sm dark:bg-slate-950"><SectionTitle title="LOB Device Daily" sub="Sumber RAW SalesPerson AB–AR"/><Table><TableHeader><TableRow><TableHead>Nama</TableHead>{["iPhone","MacBook","iPad","Watch","AirPods"].map(x=><TableHead key={x} className="text-right">{x}</TableHead>)}</TableRow></TableHeader><TableBody>{rows.map(r=><TableRow key={r.id}><TableCell><b>{r.name}</b></TableCell><TableCell className="text-right">{r.lob.iphone}</TableCell><TableCell className="text-right">{r.lob.mac}</TableCell><TableCell className="text-right">{r.lob.ipad}</TableCell><TableCell className="text-right">{r.lob.watch}</TableCell><TableCell className="text-right">{r.lob.airpods}</TableCell></TableRow>)}{rows.length>0&&<TableRow className="bg-slate-50 font-black dark:bg-slate-900"><TableCell>TOTAL</TableCell><TableCell className="text-right">{t.iphone}</TableCell><TableCell className="text-right">{t.mac}</TableCell><TableCell className="text-right">{t.ipad}</TableCell><TableCell className="text-right">{t.watch}</TableCell><TableCell className="text-right">{t.airpods}</TableCell></TableRow>}</TableBody></Table></section>}
function DailyVas({rows}:{rows:DailyStaff[]}){const keys=["qoala","telkomsel","xl","indosat"] as const,t=keys.map(k=>rows.reduce((a,r)=>({qty:a.qty+r.vasDetail[k].qty,value:a.value+r.vasDetail[k].value}),{qty:0,value:0}));return <section className="rounded-2xl border bg-white shadow-sm dark:bg-slate-950"><SectionTitle title="VAS Daily" sub="Sumber RAW SalesPerson AB–AR"/><Table><TableHeader><TableRow><TableHead>Nama</TableHead>{["Qoala","Telkomsel","XL","Indosat"].map(x=><TableHead key={x} className="text-right">{x} Qty / Value</TableHead>)}</TableRow></TableHeader><TableBody>{rows.map(r=><TableRow key={r.id}><TableCell><b>{r.name}</b></TableCell>{keys.map(k=><TableCell key={k} className="text-right"><b>{r.vasDetail[k].qty}</b><div className="text-xs text-slate-400">{money.format(r.vasDetail[k].value)}</div></TableCell>)}</TableRow>)}{rows.length>0&&<TableRow className="bg-slate-50 font-black dark:bg-slate-900"><TableCell>TOTAL</TableCell>{t.map((x,i)=><TableCell key={i} className="text-right"><b>{x.qty}</b><div className="text-xs">{money.format(x.value)}</div></TableCell>)}</TableRow>}</TableBody></Table></section>}

function Totals({rows}:{rows:StaffMetric[]}){return rows.reduce((a,r)=>({target:a.target+r.targets.amount,amount:a.amount+r.amount,acc:a.acc+r.accessories,vas:a.vas+r.vas,qty:a.qty+r.qty,inv:a.inv+r.invoices}),{target:0,amount:0,acc:0,vas:0,qty:0,inv:0})}
function StaffPerformance({data,period,setPeriod,staff,setStaff,rows}:{data:M238Payload|null;period:string;setPeriod:(v:string)=>void;staff:string;setStaff:(v:string)=>void;rows:StaffMetric[]}){const t=Totals({rows});return <div className="space-y-5"><div><h1 className="text-3xl font-black">Staff Performance</h1><p className="mt-1 text-sm text-slate-500">Seluruh staff M238 kecuali supervisor dan akun online.</p></div><section className="rounded-2xl border bg-white p-4 shadow-sm dark:bg-slate-950"><div className="grid gap-4 sm:grid-cols-2"><Filter label="Bulan" value={period} onChange={setPeriod}>{months.map(x=><option key={x} value={x}>{monthName(x)}</option>)}</Filter><Filter label="Staff" value={staff} onChange={setStaff}><option value="ALL">Semua Staff</option>{data?.staff.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</Filter></div></section><section className="rounded-2xl border bg-white shadow-sm dark:bg-slate-950"><SectionTitle title="Staff Performance" sub="Achievement value bulanan"/><Table><TableHeader><TableRow><TableHead>Nama</TableHead><TableHead className="text-right">Target</TableHead><TableHead className="text-right">Achievement</TableHead><TableHead className="text-right">%</TableHead><TableHead className="text-right">Accessories</TableHead><TableHead className="text-right">VAS</TableHead><TableHead className="text-right">UPT</TableHead><TableHead className="text-right">ATV</TableHead></TableRow></TableHeader><TableBody>{rows.map(r=><TableRow key={r.id}><TableCell><b>{r.name}</b><div className="text-xs text-slate-400">{r.position}</div></TableCell><TableCell className="text-right">{money.format(r.targets.amount)}</TableCell><TableCell className="text-right">{money.format(r.amount)}</TableCell><TableCell className="text-right">{pct(ach(r.amount,r.targets.amount))}</TableCell><TableCell className="text-right">{money.format(r.accessories)}</TableCell><TableCell className="text-right">{money.format(r.vas)}</TableCell><TableCell className="text-right">{r.upt.toFixed(1)}</TableCell><TableCell className="text-right">{money.format(r.atv)}</TableCell></TableRow>)}{rows.length>0&&<TableRow className="bg-slate-50 font-black dark:bg-slate-900"><TableCell>TOTAL</TableCell><TableCell className="text-right">{money.format(t.target)}</TableCell><TableCell className="text-right">{money.format(t.amount)}</TableCell><TableCell className="text-right">{pct(ach(t.amount,t.target))}</TableCell><TableCell className="text-right">{money.format(t.acc)}</TableCell><TableCell className="text-right">{money.format(t.vas)}</TableCell><TableCell className="text-right">{(t.inv?t.qty/t.inv:0).toFixed(1)}</TableCell><TableCell className="text-right">{money.format(t.inv?t.amount/t.inv:0)}</TableCell></TableRow>}</TableBody></Table></section><MonthlyLob rows={rows}/><MonthlyVas rows={rows}/></div>}
function MonthlyLob({rows}:{rows:StaffMetric[]}){const t=rows.reduce((a,r)=>({iphone:a.iphone+r.lob.iphone,mac:a.mac+r.lob.mac,ipad:a.ipad+r.lob.ipad,watch:a.watch+r.lob.watch,airpods:a.airpods+r.lob.airpods}),{iphone:0,mac:0,ipad:0,watch:0,airpods:0});return <section className="rounded-2xl border bg-white shadow-sm dark:bg-slate-950"><SectionTitle title="Achievement by LOB" sub="Qty per staff"/><Table><TableHeader><TableRow><TableHead>Nama</TableHead><TableHead className="text-right">iPhone</TableHead><TableHead className="text-right">MacBook</TableHead><TableHead className="text-right">iPad</TableHead><TableHead className="text-right">Watch</TableHead><TableHead className="text-right">AirPods</TableHead></TableRow></TableHeader><TableBody>{rows.map(r=><TableRow key={r.id}><TableCell><b>{r.name}</b></TableCell><TableCell className="text-right">{r.lob.iphone}</TableCell><TableCell className="text-right">{r.lob.mac}</TableCell><TableCell className="text-right">{r.lob.ipad}</TableCell><TableCell className="text-right">{r.lob.watch}</TableCell><TableCell className="text-right">{r.lob.airpods}</TableCell></TableRow>)}{rows.length>0&&<TableRow className="bg-slate-50 font-black dark:bg-slate-900"><TableCell>TOTAL</TableCell><TableCell className="text-right">{t.iphone}</TableCell><TableCell className="text-right">{t.mac}</TableCell><TableCell className="text-right">{t.ipad}</TableCell><TableCell className="text-right">{t.watch}</TableCell><TableCell className="text-right">{t.airpods}</TableCell></TableRow>}</TableBody></Table></section>}
function MonthlyVas({rows}:{rows:StaffMetric[]}){const keys=["qoala","telkomsel","xl","indosat"] as const,t=keys.map(k=>rows.reduce((a,r)=>({qty:a.qty+r.vasDetail[k].qty,value:a.value+r.vasDetail[k].value}),{qty:0,value:0}));return <section className="rounded-2xl border bg-white shadow-sm dark:bg-slate-950"><SectionTitle title="Achievement VAS" sub="Value dan qty per staff"/><Table><TableHeader><TableRow><TableHead>Nama</TableHead>{["Qoala","Telkomsel","XL","Indosat"].map(x=><TableHead key={x} className="text-right">{x}</TableHead>)}</TableRow></TableHeader><TableBody>{rows.map(r=><TableRow key={r.id}><TableCell><b>{r.name}</b></TableCell>{keys.map(k=><TableCell key={k} className="text-right"><b>{r.vasDetail[k].qty}</b><div className="text-xs text-slate-400">{money.format(r.vasDetail[k].value)}</div></TableCell>)}</TableRow>)}{rows.length>0&&<TableRow className="bg-slate-50 font-black dark:bg-slate-900"><TableCell>TOTAL</TableCell>{t.map((x,i)=><TableCell key={i} className="text-right"><b>{x.qty}</b><div className="text-xs">{money.format(x.value)}</div></TableCell>)}</TableRow>}</TableBody></Table></section>}
function Incentive({data,period,setPeriod}:{data:M238Payload|null;period:string;setPeriod:(v:string)=>void}){const rows=data?.monthlyStaff??[],total=rows.reduce((a,r)=>a+r.incentive.total,0);return <div className="space-y-5"><div><h1 className="text-3xl font-black">Estimasi Incentive Staff</h1><p className="mt-1 text-sm text-slate-500">Breakdown seluruh staff M238.</p></div><PeriodFilter period={period} setPeriod={setPeriod}/><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Card label="Total Est. Incentive" value={money.format(total)} sub={`${rows.length} staff`} tone="green"/><Card label="Device" value={money.format(rows.reduce((a,r)=>a+r.incentive.mac+r.incentive.iphone+r.incentive.ipad+r.incentive.watch,0))} sub="Mac, iPhone, iPad, Watch"/><Card label="Accessories" value={money.format(rows.reduce((a,r)=>a+r.incentive.accessories,0))} sub="Per item berdasarkan tier" tone="violet"/><Card label="Qoala" value={money.format(rows.reduce((a,r)=>a+r.incentive.qoala,0))} sub="Estimasi incentive Qoala" tone="amber"/></section><section className="rounded-2xl border bg-white shadow-sm dark:bg-slate-950"><SectionTitle title="Breakdown Incentive" sub="Estimasi per staff"/><Table><TableHeader><TableRow><TableHead>Nama</TableHead><TableHead className="text-right">Mac</TableHead><TableHead className="text-right">iPhone</TableHead><TableHead className="text-right">iPad</TableHead><TableHead className="text-right">Watch</TableHead><TableHead className="text-right">Accessories</TableHead><TableHead className="text-right">Qoala</TableHead><TableHead className="text-right">Total</TableHead></TableRow></TableHeader><TableBody>{rows.map(r=><TableRow key={r.id}><TableCell><b>{r.name}</b><div className="text-xs text-slate-400">{r.position}</div></TableCell><TableCell className="text-right">{money.format(r.incentive.mac)}</TableCell><TableCell className="text-right">{money.format(r.incentive.iphone)}</TableCell><TableCell className="text-right">{money.format(r.incentive.ipad)}</TableCell><TableCell className="text-right">{money.format(r.incentive.watch)}</TableCell><TableCell className="text-right">{money.format(r.incentive.accessories)}</TableCell><TableCell className="text-right">{money.format(r.incentive.qoala)}</TableCell><TableCell className="text-right font-black">{money.format(r.incentive.total)}</TableCell></TableRow>)}</TableBody></Table></section></div>}

function Feedback({period}:{period:string}){const[date,setDate]=useState(today()),[records,setRecords]=useState<FeedbackRecord[]>([]),[daily,setDaily]=useState<DailyPayload|null>(null),[selected,setSelected]=useState(""),[category,setCategory]=useState("external"),[text,setText]=useState(""),[saving,setSaving]=useState(false);const load=useCallback(async()=>{const[d,f]=await Promise.all([fetch(`/api/daily?date=${date}&t=${Date.now()}`,{cache:"no-store"}).then(r=>r.json()),fetch(`/api/feedback?period=${date.slice(0,7)}&date=${date}`).then(r=>r.json())]);setDaily(d);setRecords(f.rows??[])},[date]);useEffect(()=>{void load()},[load]);const need=(daily?.staff??[]).filter(r=>r.targets.amount>0&&ach(r.amount,r.targets.amount)<100&&!records.some(f=>f.staffId===r.id&&f.date===date));const save=async()=>{const p=(daily?.staff??[]).find(x=>x.id===selected);if(!p||!text.trim())return;setSaving(true);await fetch("/api/feedback",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({date,staffId:p.id,name:p.name,category,feedback:text})});setText("");setSelected("");await load();setSaving(false)};return <div className="space-y-5"><div><h1 className="text-3xl font-black">Feedback</h1><p className="mt-1 text-sm text-slate-500">Mengikuti staff yang benar-benar in-charge pada tanggal terpilih. Schedule tanggal yang sudah dibuka akan dikunci sebagai snapshot.</p></div><section className="rounded-2xl border bg-white p-4 shadow-sm dark:bg-slate-950"><label><span className="mb-1.5 block text-xs font-bold uppercase tracking-[.12em] text-slate-400">Tanggal</span><input type="date" value={date} onChange={e=>setDate(e.target.value)} className="h-11 w-full max-w-xs rounded-xl border bg-white px-3 dark:bg-slate-900"/></label></section><section className="grid gap-5 xl:grid-cols-[1fr_1.2fr]"><div className="rounded-2xl border bg-white shadow-sm dark:bg-slate-950"><SectionTitle title="Need Feedback" sub={`${need.length} staff belum mengisi`}/><div className="divide-y">{need.map(r=><button key={r.id} onClick={()=>setSelected(r.id)} className={`flex w-full items-center justify-between p-4 text-left ${selected===r.id?"bg-blue-50":""}`}><div><b>{r.name}</b><p className="text-xs text-slate-400">{pct(ach(r.amount,r.targets.amount))} achievement</p></div><span className="rounded-full bg-rose-50 px-2.5 py-1 text-xs font-bold text-rose-600">Need Feedback</span></button>)}{!need.length&&<p className="p-8 text-center text-sm text-slate-400">Tidak ada staff yang membutuhkan feedback.</p>}</div></div><div className="rounded-2xl border bg-white p-5 shadow-sm dark:bg-slate-950"><h3 className="font-extrabold">Input Feedback</h3><div className="mt-4 space-y-4"><Filter label="Nama Staff" value={selected} onChange={setSelected}><option value="">Pilih staff</option>{need.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</Filter><Filter label="Category" value={category} onChange={setCategory}><option value="external">Faktor External</option><option value="promo">Promo yang Berjalan</option><option value="performance">Staff yang Tidak Perform</option><option value="stock">Ketersediaan Stok</option></Filter><textarea value={text} onChange={e=>setText(e.target.value)} rows={5} className="w-full rounded-xl border p-3 dark:bg-slate-900" placeholder="Tuliskan reason…"/><button disabled={saving||!selected||!text.trim()} onClick={()=>void save()} className="w-full rounded-xl bg-blue-600 px-4 py-3 font-bold text-white disabled:opacity-50">{saving?"Menyimpan…":"Simpan Feedback"}</button></div></div></section><section className="rounded-2xl border bg-white shadow-sm dark:bg-slate-950"><SectionTitle title="Feedback Tersimpan" sub="Histori berdasarkan tanggal"/><div className="divide-y">{records.map((r,i)=><div key={`${r.timestamp}-${i}`} className="p-4"><div className="flex justify-between gap-3"><b>{r.name}</b><span className="rounded-full bg-blue-50 px-2.5 py-1 text-xs font-bold text-blue-600">{r.category}</span></div><p className="mt-2 text-sm text-slate-600 dark:text-slate-300">{r.professional}</p></div>)}{!records.length&&<p className="p-8 text-center text-sm text-slate-400">Belum ada feedback.</p>}</div></section></div>}

function CxMember({data,period}:{data:M238Payload|null;period:string}){const[date,setDate]=useState(today()),[rows,setRows]=useState<CxMemberRecord[]>([]),[staffId,setStaffId]=useState(""),[cx,setCx]=useState(0),[member,setMember]=useState(0),[saving,setSaving]=useState(false);const load=useCallback(async()=>{const r=await fetch(`/api/cx-member?period=${date.slice(0,7)}&date=${date}`),j=await r.json();setRows(j.rows??[])},[date]);useEffect(()=>{void load()},[load]);const save=async()=>{const p=data?.staff.find(x=>x.id===staffId);if(!p)return;setSaving(true);await fetch("/api/cx-member",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({date,staffId:p.id,name:p.name,cx,member})});setCx(0);setMember(0);await load();setSaving(false)};return <div className="space-y-5"><div><h1 className="text-3xl font-black">NPS/CX & Member</h1><p className="mt-1 text-sm text-slate-500">Input harian dan histori per staff.</p></div><section className="grid gap-4 sm:grid-cols-2"><Card label="CX" value={num.format(rows.reduce((a,x)=>a+x.cx,0))} sub={date}/><Card label="New Member" value={num.format(rows.reduce((a,x)=>a+x.member,0))} sub={date} tone="green"/></section><section className="grid gap-5 xl:grid-cols-[.9fr_1.3fr]"><div className="rounded-2xl border bg-white p-5 shadow-sm dark:bg-slate-950"><div className="space-y-4"><input type="date" value={date} onChange={e=>setDate(e.target.value)} className="h-11 w-full rounded-xl border px-3 dark:bg-slate-900"/><Filter label="Nama Staff" value={staffId} onChange={setStaffId}><option value="">Pilih staff</option>{data?.staff.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</Filter><div className="grid grid-cols-2 gap-3"><input type="number" min={0} value={cx} onChange={e=>setCx(Number(e.target.value))} className="h-11 rounded-xl border px-3 dark:bg-slate-900" placeholder="CX"/><input type="number" min={0} value={member} onChange={e=>setMember(Number(e.target.value))} className="h-11 rounded-xl border px-3 dark:bg-slate-900" placeholder="Member"/></div><button disabled={!staffId||saving} onClick={()=>void save()} className="w-full rounded-xl bg-blue-600 px-4 py-3 font-bold text-white disabled:opacity-50">Simpan</button></div></div><section className="rounded-2xl border bg-white shadow-sm dark:bg-slate-950"><SectionTitle title="List Harian" sub="CX dan New Member"/><Table><TableHeader><TableRow><TableHead>Nama</TableHead><TableHead className="text-right">CX</TableHead><TableHead className="text-right">Member</TableHead></TableRow></TableHeader><TableBody>{rows.map((r,i)=><TableRow key={`${r.timestamp}-${i}`}><TableCell><b>{r.name}</b></TableCell><TableCell className="text-right">{r.cx}</TableCell><TableCell className="text-right">{r.member}</TableCell></TableRow>)}</TableBody></Table></section></section></div>}
function DataUpload({upload,saveInvoice,fileRef,invoice,setInvoice,saving,uploadStatus,message}:{upload:()=>Promise<void>;saveInvoice:()=>Promise<void>;fileRef:React.RefObject<HTMLInputElement|null>;invoice:string;setInvoice:(v:string)=>void;saving:boolean;uploadStatus:string;message:string}){return <div className="space-y-5"><div><h1 className="text-3xl font-black">Data Upload</h1></div><section className="grid gap-5 lg:grid-cols-2"><div className="rounded-2xl border bg-white p-5 shadow-sm dark:bg-slate-950"><FileSpreadsheet className="size-5 text-blue-600"/><h3 className="mt-2 font-extrabold">Upload SPW</h3><input ref={fileRef} type="file" accept=".xlsx,.xls,.csv" className="mt-5 block w-full rounded-xl border p-3 text-sm"/><button disabled={saving} onClick={()=>void upload()} className="mt-3 w-full rounded-xl bg-blue-600 px-4 py-3 font-bold text-white">{uploadStatus||"Upload SPW"}</button></div><div className="rounded-2xl border bg-white p-5 shadow-sm dark:bg-slate-950"><WalletCards className="size-5 text-violet-600"/><h3 className="mt-2 font-extrabold">No Invoice Exchange</h3><input value={invoice} onChange={e=>setInvoice(e.target.value)} className="mt-5 h-12 w-full rounded-xl border px-3 dark:bg-slate-900" placeholder="Nomor invoice"/><button disabled={saving||!invoice.trim()} onClick={()=>void saveInvoice()} className="mt-3 w-full rounded-xl bg-violet-600 px-4 py-3 font-bold text-white">Simpan Invoice</button></div></section>{message&&<div className="rounded-xl bg-blue-50 p-4 text-sm font-semibold text-blue-700">{message}</div>}</div>}

function SettingsPage({data,dark,toggleTheme,refresh,setRefresh,theme,setTheme,font,setFont}:{data:M238Payload|null;dark:boolean;toggleTheme:()=>void;refresh:RefreshSetting;setRefresh:(v:RefreshSetting)=>void;theme:ThemeStyle;setTheme:(v:ThemeStyle)=>void;font:FontSize;setFont:(v:FontSize)=>void}){const[diagnosis,setDiagnosis]=useState<string[]>([]);const diagnose=()=>{const d:string[]=[];if(data?.mode!=="live")d.push("Koneksi data tidak LIVE. Periksa Google service account dan permission sheet.");if(!data?.monthlyStaff?.length)d.push("Data staff kosong. Periksa Config dan Data Copas.");if(data?.latestDate&&data.latestDate<today())d.push(`Data penjualan terakhir ${data.latestDate}; cek upload RAW SalesPerson untuk hari ini.`);if(!d.length)d.push("Semua pemeriksaan utama normal: koneksi LIVE, staff terbaca, dan data tersedia.");setDiagnosis(d)};return <div className="space-y-5"><div><h1 className="text-3xl font-black">Settings</h1><p className="mt-1 text-sm text-slate-500">Atur refresh, font, tampilan, dan bantuan dashboard.</p></div><section className="grid gap-5 xl:grid-cols-3"><div className="rounded-2xl border bg-white p-5 shadow-sm dark:bg-slate-950"><h3 className="font-extrabold">Auto Refresh</h3><div className="mt-4"><Filter label="Interval" value={refresh} onChange={v=>setRefresh(v as RefreshSetting)}><option value="auto">Auto</option><option value="60000">1 menit</option><option value="300000">5 menit</option><option value="600000">10 menit</option></Filter></div></div><div className="rounded-2xl border bg-white p-5 shadow-sm dark:bg-slate-950"><h3 className="font-extrabold">Ukuran Font</h3><div className="mt-4"><Filter label="Font" value={font} onChange={v=>setFont(v as FontSize)}><option value="small">Kecil</option><option value="normal">Normal</option><option value="large">Besar</option></Filter></div></div><div className="rounded-2xl border bg-white p-5 shadow-sm dark:bg-slate-950"><h3 className="font-extrabold">Light / Dark</h3><button onClick={toggleTheme} className="mt-4 w-full rounded-xl bg-slate-900 px-4 py-3 font-bold text-white dark:bg-white dark:text-slate-900">{dark?"Mode Terang":"Mode Gelap"}</button></div></section><section className="rounded-2xl border bg-white p-5 shadow-sm dark:bg-slate-950"><h3 className="font-extrabold">Tampilan Dashboard</h3><div className="mt-4 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{([['classic','Classic','from-blue-500 to-blue-700'],['natural','Natural','from-emerald-400 to-teal-600'],['worklife','Worklife','from-slate-700 to-blue-900'],['happiness','Happiness','from-orange-400 via-rose-500 to-violet-600']] as const).map(([k,l,g])=><button key={k} onClick={()=>setTheme(k)} className={`rounded-2xl border p-4 text-left ${theme===k?"ring-2 ring-blue-300":""}`}><div className={`h-10 rounded-xl bg-gradient-to-r ${g}`}/><b className="mt-3 block">{l}</b></button>)}</div></section><section className="rounded-2xl border bg-white p-5 shadow-sm dark:bg-slate-950"><div className="flex items-start gap-3"><span className="rounded-2xl bg-violet-50 p-3 text-violet-600"><Bot className="size-6"/></span><div className="flex-1"><h3 className="font-extrabold">Robot AI Help</h3><p className="mt-1 text-sm text-slate-500">Asisten diagnostik untuk membantu menemukan masalah dashboard.</p><button onClick={diagnose} className="mt-4 flex items-center gap-2 rounded-xl bg-violet-600 px-4 py-2.5 text-sm font-bold text-white"><WandSparkles className="size-4"/>Diagnose Dashboard</button>{diagnosis.length>0&&<div className="mt-4 space-y-2">{diagnosis.map((x,i)=><div key={i} className="rounded-xl bg-slate-50 p-3 text-sm dark:bg-slate-900">{x}</div>)}</div>}</div></div></section></div>}
