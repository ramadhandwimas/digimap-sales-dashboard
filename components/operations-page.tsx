"use client";
import {useEffect,useState} from "react";
const money=new Intl.NumberFormat("id-ID",{style:"currency",currency:"IDR",maximumFractionDigits:0}),today=()=>new Intl.DateTimeFormat("sv-SE",{timeZone:"Asia/Jakarta",year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date());
const box="rounded-2xl border bg-white p-5 shadow-sm dark:bg-slate-950",input="h-11 rounded-xl border bg-white px-3 dark:bg-slate-900";
type Agg={qty:number;amount:number};
export default function OperationsPage({mode}:{mode:"bnpl"|"soh"|"weekly"}){return <main className="min-h-screen bg-slate-50 p-4 text-slate-950 dark:bg-slate-900 dark:text-slate-100 sm:p-7"><a href="/" className="text-sm font-bold text-blue-600">← Kembali ke Dashboard</a>{mode==="bnpl"?<Bnpl/>:mode==="soh"?<Soh/>:<Weekly/>}</main>}
function Bnpl(){const[d,setD]=useState(today()),[period,setPeriod]=useState(today().slice(0,7)),[group,setGroup]=useState("BNPL"),[type,setType]=useState("HCI"),[qty,setQty]=useState(1),[amount,setAmount]=useState(0),[rows,setRows]=useState<any[]>([]),[msg,setMsg]=useState("");const load=async()=>setRows((await (await fetch(`/api/bnpl?period=${period}`)).json()).rows||[]);useEffect(()=>{void load()},[period]);useEffect(()=>setType(group==="BNPL"?"HCI":"Laku6 Master Device"),[group]);const save=async()=>{const r=await fetch("/api/bnpl",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({date:d,group,type,qty,amount})}),j=await r.json();setMsg(r.ok?"Data berhasil disimpan.":j.error);if(r.ok){setAmount(0);setQty(1);await load()}};const types=group==="BNPL"?["HCI","Indodana","Kredivo","Akulaku","SPaylater"]:["Laku6 Master Device","OnePulse"];return <div className="mt-6 space-y-5"><div><h1 className="text-3xl font-black">BNPL & Trade-In</h1><p className="text-slate-500">Input manual tersimpan permanen dan dapat dilihat kembali per bulan.</p></div><section className={box}><div className="grid gap-3 md:grid-cols-6"><input className={input} type="date" value={d} onChange={e=>setD(e.target.value)}/><select className={input} value={group} onChange={e=>setGroup(e.target.value)}><option>BNPL</option><option value="TRADE_IN">TRADE IN</option></select><select className={input} value={type} onChange={e=>setType(e.target.value)}>{types.map(x=><option key={x}>{x}</option>)}</select><input className={input} type="number" min="0" value={qty} onChange={e=>setQty(Number(e.target.value))} placeholder="Qty"/><input className={input} type="number" min="0" value={amount} onChange={e=>setAmount(Number(e.target.value))} placeholder="Amount"/><button onClick={save} className="rounded-xl bg-blue-600 px-4 font-bold text-white">Simpan</button></div>{msg&&<p className="mt-3 text-sm font-semibold">{msg}</p>}</section><section className={box}><div className="mb-4 flex items-center justify-between gap-3"><h2 className="font-black">Daily Tracking</h2><input type="month" value={period} onChange={e=>setPeriod(e.target.value)} className={input}/></div><div className="overflow-x-auto"><table className="min-w-[900px] w-full text-sm"><thead><tr className="border-b text-left"><th className="p-2">Tanggal</th><th>Hari</th><th>Group</th><th>Type</th><th>Qty</th><th>Amount</th></tr></thead><tbody>{rows.map((r,i)=><tr className="border-b" key={i}><td className="p-2">{r.date}</td><td>{r.day}</td><td>{r.group}</td><td className="font-bold">{r.type}</td><td>{r.qty}</td><td>{money.format(r.amount)}</td></tr>)}</tbody></table></div></section></div>}
function Soh(){const[q,setQ]=useState(""),[lob,setLob]=useState("ALL"),[rows,setRows]=useState<any[]>([]);useEffect(()=>{const t=setTimeout(async()=>setRows((await (await fetch(`/api/soh?lob=${encodeURIComponent(lob)}&q=${encodeURIComponent(q)}`)).json()).rows||[]),250);return()=>clearTimeout(t)},[q,lob]);return <div className="mt-6 space-y-5"><div><h1 className="text-3xl font-black">SOH Stock All Device</h1><p className="text-slate-500">Stock dari RAW StockPosition kolom A–D.</p></div><section className={box}><div className="grid gap-3 sm:grid-cols-2"><select className={input} value={lob} onChange={e=>setLob(e.target.value)}>{["ALL","IPHONE","IPAD","MACBOOK","APPLE WATCH","AIRPODS","PENCIL","KEYBOARD"].map(x=><option key={x}>{x}</option>)}</select><input className={input} value={q} onChange={e=>setQ(e.target.value)} placeholder="Cari article atau description..."/></div></section><section className={box}><div className="overflow-x-auto"><table className="w-full min-w-[650px] text-sm"><thead><tr className="border-b text-left"><th className="p-2">LOB</th><th>Article</th><th>Description</th><th>Qty</th></tr></thead><tbody>{rows.map((r,i)=><tr key={i} className="border-b"><td className="p-2 font-bold">{r.category}</td><td>{r.article}</td><td>{r.description}</td><td className="font-black">{r.qty}</td></tr>)}</tbody></table></div></section></div>}
function Weekly(){const[a,setA]=useState(8),[b,setB]=useState(9),[data,setData]=useState<any>(null);useEffect(()=>{void(async()=>setData(await (await fetch(`/api/weekly?a=${a}&b=${b}`)).json()))()},[a,b]);const compare=(x=0,y=0)=>x?`${((y-x)/x*100).toFixed(0)}%`:y?"NEW":"0%";const rows=(left:Record<string,Agg>={},right:Record<string,Agg>={})=>Array.from(new Set([...Object.keys(left),...Object.keys(right)])).map(k=>({k,x:left[k]||{qty:0,amount:0},y:right[k]||{qty:0,amount:0}}));return <div className="mt-6 space-y-5"><div><h1 className="text-3xl font-black">Weekly Report M238</h1><p className="text-slate-500">Compare Week to Week dari Data Copas.</p></div><section className={box}><div className="flex flex-wrap gap-3"><select className={input} value={a} onChange={e=>setA(Number(e.target.value))}>{Array.from({length:13},(_,i)=>i+1).map(x=><option key={x} value={x}>Week {x} Q4</option>)}</select><span className="self-center font-black">VS</span><select className={input} value={b} onChange={e=>setB(Number(e.target.value))}>{Array.from({length:13},(_,i)=>i+1).map(x=><option key={x} value={x}>Week {x} Q4</option>)}</select></div></section>{[["Sales Summary",data?.a?.scheme,data?.b?.scheme],["VAS - Qoala / Indosat / Telkomsel / XL",data?.a?.vas,data?.b?.vas]].map(([title,l,r]:any)=><section className={box} key={title}><h2 className="mb-3 font-black">{title}</h2><CompareTable rows={rows(l,r)} a={a} b={b} compare={compare}/></section>)}{["AIRPODS","IPHONE","IPAD","MACBOOK","WATCH"].map(lob=><section className={box} key={lob}><h2 className="mb-3 font-black">{lob}</h2><CompareTable rows={rows(data?.a?.lob?.[lob],data?.b?.lob?.[lob])} a={a} b={b} compare={compare}/></section>)}</div>}
function CompareTable({rows,a,b,compare}:{rows:any[];a:number;b:number;compare:(x:number,y:number)=>string}){return <div className="overflow-x-auto"><table className="min-w-[850px] w-full text-sm"><thead><tr className="border-b text-left"><th className="p-2">Category / Type</th><th>W{a} Qty</th><th>W{a} Amount</th><th>W{b} Qty</th><th>W{b} Amount</th><th>Qty %</th><th>Amount %</th></tr></thead><tbody>{rows.map(r=><tr className="border-b" key={r.k}><td className="p-2 font-bold">{r.k}</td><td>{r.x.qty}</td><td>{money.format(r.x.amount)}</td><td>{r.y.qty}</td><td>{money.format(r.y.amount)}</td><td className={compare(r.x.qty,r.y.qty).startsWith("-")?"text-red-600 font-bold":"text-emerald-600 font-bold"}>{compare(r.x.qty,r.y.qty)}</td><td className={compare(r.x.amount,r.y.amount).startsWith("-")?"text-red-600 font-bold":"text-emerald-600 font-bold"}>{compare(r.x.amount,r.y.amount)}</td></tr>)}</tbody></table></div>}