"use client";import {useEffect,useState} from "react";const money=new Intl.NumberFormat("id-ID",{style:"currency",currency:"IDR",maximumFractionDigits:0}),today=()=>new Intl.DateTimeFormat("sv-SE",{timeZone:"Asia/Jakarta",year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date()),box="rounded-2xl border bg-white p-5 shadow-sm dark:bg-slate-950",input="h-11 rounded-xl border bg-white px-3 dark:bg-slate-900";type Agg={qty:number;amount:number};type WeeklySide={scheme:Record<string,Agg>;vas:Record<string,Agg>;lob:Record<string,Record<string,Agg>>};type WeeklyPayload={weekA:number;weekB:number;labelA:string;labelB:string;a:WeeklySide;b:WeeklySide;reasons:Record<string,string>};export default function OperationsPage({mode}:{mode:"bnpl"|"soh"|"weekly"}){return <main className="min-h-screen bg-slate-50 p-4 text-slate-950 dark:bg-slate-900 dark:text-slate-100 sm:p-7"><a href="/" className="text-sm font-bold text-blue-600">← Kembali ke Dashboard</a>{mode==="bnpl"?<Bnpl/>:mode==="soh"?<Soh/>:<Weekly/>}</main>}
function Bnpl(){const[d,setD]=useState(today()),[period,setPeriod]=useState(today().slice(0,7)),[group,setGroup]=useState("BNPL"),[type,setType]=useState("HCI"),[qty,setQty]=useState(1),[amount,setAmount]=useState(0),[rows,setRows]=useState<any[]>([]),[msg,setMsg]=useState("");const load=async()=>setRows((await (await fetch(`/api/bnpl?period=${period}`)).json()).rows||[]);useEffect(()=>{void load()},[period]);useEffect(()=>setType(group==="BNPL"?"HCI":"Laku6 Master Device"),[group]);const save=async()=>{const r=await fetch("/api/bnpl",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({date:d,group,type,qty,amount})}),j=await r.json();setMsg(r.ok?"Data berhasil disimpan.":j.error);if(r.ok){setAmount(0);setQty(1);await load()}};const types=group==="BNPL"?["HCI","Indodana","Kredivo","Akulaku","SPaylater"]:["Laku6 Master Device","OnePulse"];return <div className="mt-6 space-y-5"><h1 className="text-3xl font-black">BNPL & Trade-In</h1><section className={box}><div className="grid gap-3 md:grid-cols-6"><input className={input} type="date" value={d} onChange={e=>setD(e.target.value)}/><select className={input} value={group} onChange={e=>setGroup(e.target.value)}><option>BNPL</option><option value="TRADE_IN">TRADE IN</option></select><select className={input} value={type} onChange={e=>setType(e.target.value)}>{types.map(x=><option key={x}>{x}</option>)}</select><input className={input} type="number" value={qty} onChange={e=>setQty(+e.target.value)}/><input className={input} type="number" value={amount} onChange={e=>setAmount(+e.target.value)}/><button onClick={save} className="rounded-xl bg-blue-600 font-bold text-white">Simpan</button></div>{msg&&<p className="mt-3">{msg}</p>}</section><section className={box}><div className="mb-4 flex justify-between"><h2 className="font-black">Daily Tracking</h2><input type="month" value={period} onChange={e=>setPeriod(e.target.value)} className={input}/></div><div className="overflow-x-auto"><table className="min-w-[850px] w-full text-sm"><thead><tr><th>Tanggal</th><th>Hari</th><th>Category</th><th>Type</th><th>Qty</th><th>Amount</th></tr></thead><tbody>{rows.map((r,i)=><tr className="border-t" key={i}><td className="p-2">{r.date}</td><td>{r.day}</td><td>{r.group}</td><td>{r.type}</td><td>{r.qty}</td><td>{money.format(r.amount)}</td></tr>)}</tbody></table></div></section></div>}
function Soh(){const[q,setQ]=useState(""),[rows,setRows]=useState<any[]>([]),[updated,setUpdated]=useState("");useEffect(()=>{const t=setTimeout(async()=>{const j=await (await fetch(`/api/soh?q=${encodeURIComponent(q)}`)).json();setRows(j.rows||[]);setUpdated(j.updated||"")},200);return()=>clearTimeout(t)},[q]);const groups=[["IPHONE","iPhone"],["IPAD","iPad"],["MACBOOK","Mac"],["APPLE WATCH","Apple Watch"],["AIRPODS, PENCIL & KEYBOARD","AirPods, Pencil & Keyboard"]] as const;return <div className="mt-6 space-y-5"><div><h1 className="text-3xl font-black">Stock On Hand</h1><p className="text-slate-500">Isi mengikuti langsung sheet SOH{updated?` • Updated ${updated}`:""}.</p></div><section className={box}><input className={`${input} w-full`} value={q} onChange={e=>setQ(e.target.value)} placeholder="Cari Article atau Description..."/></section><div className="grid gap-5 xl:grid-cols-2">{groups.map(([key,label])=>{const rr=rows.filter(x=>x.category===key),total=rr.reduce((a,x)=>a+x.qty,0);return <section className={box} key={key}><div className="mb-3 flex justify-between"><h2 className="font-black">{label}</h2><b>Grand Total: {total}</b></div><div className="max-h-[520px] overflow-auto"><table className="w-full text-sm"><thead className="sticky top-0 bg-white dark:bg-slate-950"><tr><th className="p-2 text-left">Article</th><th className="text-left">Description</th><th className="text-right">Qty</th></tr></thead><tbody>{rr.map((r:any)=><tr className="border-t" key={`${r.article}-${r.description}`}><td className="p-2 font-semibold">{r.article}</td><td>{r.description}</td><td className="text-right font-black">{r.qty}</td></tr>)}</tbody></table></div></section>})}</div></div>}
function Weekly(){const[data,setData]=useState<WeeklyPayload|null>(null);useEffect(()=>{let active=true;const load=async()=>{const response=await fetch("/api/weekly",{cache:"no-store"}),json=await response.json();if(active)setData(json)};void load();const timer=setInterval(()=>void load(),300000);return()=>{active=false;clearInterval(timer)}},[]);const compare=(x=0,y=0)=>x?`${((y-x)/x*100).toFixed(0)}%`:y?"NEW":"0%",merge=(left:Record<string,Agg>={},right:Record<string,Agg>={})=>Array.from(new Set([...Object.keys(left),...Object.keys(right)])).map(k=>({k,x:left[k]||{qty:0,amount:0},y:right[k]||{qty:0,amount:0}})),sum=(values:Record<string,Agg>={})=>Object.values(values).reduce((total,value)=>({qty:total.qty+value.qty,amount:total.amount+value.amount}),{qty:0,amount:0}),lobs=["AIRPODS","IPHONE","MAC","IPAD","APPLE WATCH"],names:Record<string,string>={AIRPODS:"AirPods",IPHONE:"iPhone",MAC:"MacBook",IPAD:"iPad","APPLE WATCH":"Watch"},la=(data?.labelA??"Week sebelumnya").replace("Week ","W"),lb=(data?.labelB??"Week berjalan").replace("Week ","W");return <div className="mt-6 space-y-5"><div><h1 className="text-3xl font-black">Weekly Report M238</h1><p className="mt-1 text-sm text-slate-500">Perbandingan otomatis mengikuti dua week terbaru di Data Copas.</p></div><section className={box}><div className="grid gap-3 sm:grid-cols-[1fr_auto_1fr] sm:items-center"><div className="rounded-xl bg-slate-50 p-4 dark:bg-slate-900"><p className="text-xs font-bold uppercase tracking-wide text-slate-400">Week Sebelumnya</p><b className="mt-2 block text-lg">{data?.labelA??"Memuat…"}</b></div><b className="text-center text-blue-600">VS</b><div className="rounded-xl bg-blue-50 p-4 dark:bg-blue-950/30"><p className="text-xs font-bold uppercase tracking-wide text-blue-500">Week Berjalan</p><b className="mt-2 block text-lg">{data?.labelB??"Memuat…"}</b></div></div><p className="mt-3 text-sm text-slate-500">Sumber: Data Copas • Comparison akan bergeser otomatis saat data week berikutnya tersedia.</p></section><section className={box}><h2 className="mb-3 font-black">Sales Summary — Device / VAS / ACC</h2><CompareTotal rows={merge(data?.a?.scheme,data?.b?.scheme)} la={la} lb={lb} compare={compare} totalLabel="M238 TOTAL"/></section><section className={box}><h2 className="mb-3 font-black">VAS — Qoala / Indosat / Telkomsel / XL</h2><CompareTotal rows={merge(data?.a?.vas,data?.b?.vas)} la={la} lb={lb} compare={compare} totalLabel="VAS TOTAL"/></section><section className={box}><div className="mb-3"><h2 className="font-black">Weekly LOB M238</h2><p className="text-sm text-slate-500">Per type dengan comparison otomatis {data?.labelA??"week sebelumnya"} vs {data?.labelB??"week berjalan"}.</p></div><div className="overflow-x-auto"><table className="w-full min-w-[940px] text-sm"><thead><tr><th className="p-2 text-left">Product Category</th><th className="text-left">Type</th><th>{la} Qty</th><th>{la} Amount</th><th>{lb} Qty</th><th>{lb} Amount</th><th>Qty %</th><th>Amount %</th></tr></thead><tbody>{lobs.flatMap(lob=>{const rows=merge(data?.a?.lob?.[lob],data?.b?.lob?.[lob]),a=sum(data?.a?.lob?.[lob]),b=sum(data?.b?.lob?.[lob]);return[...rows.map((row,index)=><tr className="border-t" key={`${lob}-${row.k}`}><td className="p-2 font-black">{index===0?names[lob]:""}</td><td className="font-semibold">{row.k}</td><td className="text-center">{row.x.qty}</td><td>{money.format(row.x.amount)}</td><td className="text-center">{row.y.qty}</td><td>{money.format(row.y.amount)}</td><td>{compare(row.x.qty,row.y.qty)}</td><td>{compare(row.x.amount,row.y.amount)}</td></tr>),<tr className="border-t bg-slate-50 font-black dark:bg-slate-900" key={`${lob}-total`}><td className="p-2">{names[lob]} Total</td><td/><td className="text-center">{a.qty}</td><td>{money.format(a.amount)}</td><td className="text-center">{b.qty}</td><td>{money.format(b.amount)}</td><td>{compare(a.qty,b.qty)}</td><td>{compare(a.amount,b.amount)}</td></tr>]})}</tbody></table></div><div className="mt-6 border-t pt-5"><h3 className="font-black">Reason Weekly per LOB</h3><p className="mt-1 text-sm text-slate-500">Analisa otomatis berdasarkan perubahan qty, amount, dan tipe produk.</p><div className="mt-4 grid gap-4 lg:grid-cols-2">{lobs.map(lob=><article key={lob} className="rounded-2xl border bg-slate-50 p-4 dark:bg-slate-900"><h4 className="font-black text-blue-600">{names[lob]}</h4><p className="mt-2 text-sm leading-7 text-slate-700 dark:text-slate-200">{data?.reasons?.[lob]??"Menunggu data comparison week."}</p></article>)}</div></div></section></div>}
function CompareTotal({rows,la,lb,compare,totalLabel}:{rows:Array<{k:string;x:Agg;y:Agg}>;la:string;lb:string;compare:(x:number,y:number)=>string;totalLabel:string}){const total=rows.reduce((sum,row)=>({x:{qty:sum.x.qty+row.x.qty,amount:sum.x.amount+row.x.amount},y:{qty:sum.y.qty+row.y.qty,amount:sum.y.amount+row.y.amount}}),{x:{qty:0,amount:0},y:{qty:0,amount:0}});return <div className="overflow-x-auto"><table className="w-full min-w-[850px] text-sm"><thead><tr><th className="p-2 text-left">Category</th><th>{la} Qty</th><th>{la} Amount</th><th>{lb} Qty</th><th>{lb} Amount</th><th>Qty %</th><th>Amount %</th></tr></thead><tbody>{rows.map(row=><tr className="border-t" key={row.k}><td className="p-2 font-bold">{row.k}</td><td>{row.x.qty}</td><td>{money.format(row.x.amount)}</td><td>{row.y.qty}</td><td>{money.format(row.y.amount)}</td><td>{compare(row.x.qty,row.y.qty)}</td><td>{compare(row.x.amount,row.y.amount)}</td></tr>)}<tr className="border-t bg-slate-50 font-black dark:bg-slate-900"><td className="p-2">{totalLabel}</td><td>{total.x.qty}</td><td>{money.format(total.x.amount)}</td><td>{total.y.qty}</td><td>{money.format(total.y.amount)}</td><td>{compare(total.x.qty,total.y.qty)}</td><td>{compare(total.x.amount,total.y.amount)}</td></tr></tbody></table></div>}
